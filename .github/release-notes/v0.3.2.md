## YbkAuto v0.3.2

### 迁移

- 桥接旧版本自动更新到主账号 GitHub Releases。
- 内置新的 updater public key，用于后续主账号发布的版本签名验证。
- 保持更新源指向 `9ycrooked/YbkAuto`。

### 说明

- 此版本应由小号仓库发布，并使用旧 updater 私钥签名，让现有 `0.3.0` / `0.3.1` 用户可以自动升级。
- 后续 `v0.3.3+` 可以从主账号仓库发布，并使用新的 updater 私钥签名。
